# Resultados

Los resultados ya han sido obtenidos y están disponibles. Si deseas probar el programa por tu cuenta, simplemente ejecuta el script `./solver.sh` desde la terminal, proporcionando la ruta relativa a un archivo `.zpl` como argumento.

```bash
./solver.sh path/to/file.zpl
```

Asegúrate de que el archivo `.zpl` esté correctamente ubicado y accesible desde el directorio actual.